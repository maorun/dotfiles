require("copilot").setup({
    suggestion = {
        -- auto_trigger = true,
        keymap = {
            accept = "<Tab>",
            next = "<C-n>",
            prev = "<C-p>",
        }
    }
})

